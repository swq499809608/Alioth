import tensorflow as tf
from ..framework.utils import get_from_module
def exponential_decay(learning_rate, global_step, decay_steps, decay_rate, staircase=False):
	
def gradient(loss, learning_rate, global_step, decay_steps, decay_rate, staircase=False)